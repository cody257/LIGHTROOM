# LIGHT ROOM

## Agent skills

### Issue tracker

Issues live in Linear (team `Copperstateit`, one project per repo), operated via the connected Linear MCP server. See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical triage label names (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`), created in Linear on first use. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
